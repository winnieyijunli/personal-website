# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# full-size source images before the pre-launch resize pass — never
# served, kept locally only
originals-backup

# one-off local build variants for the Cloudflare Pages / GitHub Pages
# mirror deployments (different base path / OG metadata than the main
# Workers deployment main builds) — not part of the site's source
dist-pages
dist-ghpages
pages-assets
